# TV Player for VIDAA

Статическая сборка персонального IPTV-плеера для VIDAA.

- Приложение запускается напрямую из GitHub Pages.
- Адреса IPTV и медиатеки не записаны в репозиторий и не загружаются из удалённого конфига.
- При первом запуске пользователь вводит M3U URL; он сохраняется только локально на устройстве.
- Каталог `epg/` содержит подготовленные небольшие фрагменты программы передач.
- M3U и EPG открываются из локального кэша и обновляются в фоне.
- Плеер восстанавливает зависший поток, умеет перейти на SD-дубль и сохраняет позицию архива.
- В полноэкранном режиме `←` открывает быстрый переключатель соседних каналов.

Публичный URL приложения после включения GitHub Pages:

`https://<github-user>.github.io/tv-player-vidaa/`
